import React from "react";
import styles from "./Logo.module.scss";

const Logo = () => {
  return (
    <a href="#hero" className={styles.logo}>
      Viktor Musteata
    </a>
  );
};

export default Logo;
